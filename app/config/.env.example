# ==============================
# X Auto Poster .env (example)
# Copy to app/config/.env and fill values
# ==============================

# --- OpenAI ---
# Required if LLM_PROVIDER=openai
OPENAI_API_KEY=YOUR_OPENAI_API_KEY

# --- Google Gemini ---
# Required if LLM_PROVIDER=gemini (either one is fine)
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY
# or
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# --- X (Twitter) API ---
# OAuth2 (optional). If provided, used for posting when suitable.
X_OAUTH2_ACCESS_TOKEN=

# OAuth 1.0a (required for media upload flow)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# --- Misc ---
# Timezone override (optional; usually configured in config.json)
#TZ=Asia/Tokyo