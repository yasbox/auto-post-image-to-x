# 環境変数テンプレート (.env.example)
# このファイルを `app/config/.env` にコピーして値を設定してください。

# --- LLM 設定 ---
# 使用するプロバイダ（現状 'openai' のみ）
LLM_PROVIDER=openai
# 使用モデル（タイトル生成に使用）
LLM_MODEL=gpt-4o-mini
# OpenAI API キー（LLM_PROVIDER=openai の場合に必須）
OPENAI_API_KEY=

# --- X (Twitter) 認証情報 ---
# OAuth 2.0 ユーザーコンテキストトークン（任意。あれば /2/tweets に優先使用）
X_OAUTH2_ACCESS_TOKEN=

# OAuth 1.0a 資格情報（media/upload v1.1 に必須）
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
